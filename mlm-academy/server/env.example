# Пример переменных. Скопировать в секрет-хранилище, не коммитить заполненный файл.

# Account API (Cloudflare Worker). Только сервер.
MLMA_SESSION_SECRET=
MLMA_API_PUBLIC_URL=

# Supabase Auth. Пока не подключено — оставить пустыми, не выдумывать значения.
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Webhook оплаты. Не класть в Tilda.
MLMA_WEBHOOK_SECRET=
PAYMENT_GATEWAY=test
