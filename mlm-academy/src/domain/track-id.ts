/**
 * Формат стабильного Track ID.
 *
 * Живёт отдельным крошечным модулем намеренно: маршруты нужны и на клиенте,
 * а тянуть ради одной регулярки схемы валидации в клиентский bundle нельзя.
 */
export const TRACK_ID_PATTERN = /^A[1-6]-\d{3}$/;
